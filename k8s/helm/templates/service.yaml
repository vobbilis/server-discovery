apiVersion: v1
kind: Service
metadata:
  name: {{ include "server-discovery.fullname" . }}
  labels:
    {{- include "server-discovery.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "server-discovery.selectorLabels" . | nindent 4 }} 